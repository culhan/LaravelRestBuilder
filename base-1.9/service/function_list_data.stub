/**
     * [{{name}} description]
     * @return [type] [description]
     */
    public function get{{Name}}Data($data,$relation,$data_filter{{param_function}})
    {
        $dataRecord = $data_filter;

        {{data-filter}}
        
        {{data-validation}}

        // custom code before
        {{custom_code_before}}

        $return = $this->repository->getIndexData($data,$relation,$data_filter);

        // custom code after
        {{custom_code_after}}

        return $return;
    }

    // end list function